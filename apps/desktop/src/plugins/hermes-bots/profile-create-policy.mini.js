// Mini profiles start credential-free. Provider setup remains a Mini-side
// operator action and is never initiated by the renderer.
export const PROFILE_CREATE_POLICY = Object.freeze({ inherit: 'none' })
